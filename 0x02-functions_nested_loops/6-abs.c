#include "main.h"
/**
 *_abs - compute the absolute value of a number
 *@n: the int o be checked
 *Return: the absolute value of n
*/
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	return (-n);
}
